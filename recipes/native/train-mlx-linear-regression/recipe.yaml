name: train/mlx-linear-regression
version: v1
description: Train a two-parameter linear model with MLX on a Native Metal host.
task: train
backend: native
runtime: mlx
manifest: native-workload.yaml
parametersSchema: parameters.schema.json
example: example.yaml
prerequisites:
- A joined Native Metal host with sandboxed shell access.
- MLX 0.32.0 installed at /var/tmp/idleloom-mlx/bin/python.
