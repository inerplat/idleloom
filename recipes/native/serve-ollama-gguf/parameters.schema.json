{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "model": {
      "type": "string",
      "description": "Cluster-scoped digest-pinned Ollama IdleloomModel catalog entry.",
      "default": "qwen3-5-9b-ollama",
      "format": "dnsSubdomain"
    },
    "modelAlias": {
      "type": "string",
      "description": "Stable model name accepted by the OpenAI-compatible API.",
      "default": "qwen3-5-9b",
      "format": "dnsSubdomain"
    },
    "namespace": {
      "type": "string",
      "description": "Namespace for the IdleloomWorkload, Service, and generated client Secret.",
      "default": "default",
      "format": "namespace"
    },
    "servicePort": {
      "type": "integer",
      "description": "ClusterIP Service port.",
      "default": 8000,
      "minimum": 1,
      "maximum": 65535
    },
    "unifiedMemory": {
      "type": "string",
      "description": "Unified memory reservation for GGUF model loading and serving.",
      "default": "16Gi",
      "format": "positiveQuantity"
    }
  }
}
