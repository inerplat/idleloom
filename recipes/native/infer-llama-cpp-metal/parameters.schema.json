{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "maxTokens": {
      "type": "integer",
      "description": "Maximum number of tokens to generate.",
      "default": 128,
      "minimum": 1,
      "maximum": 512
    },
    "model": {
      "type": "string",
      "description": "Cluster-scoped digest-pinned llama.cpp IdleloomModel catalog entry.",
      "default": "local-gguf",
      "format": "dnsSubdomain"
    },
    "namespace": {
      "type": "string",
      "description": "Namespace for the IdleloomWorkload.",
      "default": "default",
      "format": "namespace"
    },
    "prompt": {
      "type": "string",
      "description": "Prompt passed to the pinned local GGUF model.",
      "default": "Explain why idle compute is useful in one sentence.",
      "minLength": 1,
      "maxLength": 16384
    },
    "timeoutSeconds": {
      "type": "integer",
      "description": "Maximum inference runtime after llama-server is ready.",
      "default": 600,
      "minimum": 1,
      "maximum": 3600
    },
    "unifiedMemory": {
      "type": "string",
      "description": "Unified memory reservation for GGUF model loading and inference.",
      "default": "8Gi",
      "format": "positiveQuantity"
    }
  }
}
