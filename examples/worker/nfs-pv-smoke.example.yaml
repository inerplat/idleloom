apiVersion: v1
kind: PersistentVolume
metadata:
  name: idleloom-nfs-smoke
  labels:
    app.kubernetes.io/managed-by: idleloom
    ai.idleloom.io/example: worker-nfs
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: 203.0.113.20
    path: /exports/idleloom
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: idleloom-nfs-smoke
  namespace: default
  labels:
    app.kubernetes.io/managed-by: idleloom
    ai.idleloom.io/example: worker-nfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: idleloom-nfs-smoke
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: idleloom-nfs-smoke
  namespace: default
  labels:
    app.kubernetes.io/managed-by: idleloom
    ai.idleloom.io/example: worker-nfs
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/os: linux
    idleloom-worker: "true"
  tolerations:
    - key: idleloom-dedicated
      operator: Exists
      effect: NoSchedule
  securityContext:
    fsGroup: 65532
    runAsNonRoot: true
    runAsUser: 65532
    runAsGroup: 65532
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: smoke
      image: docker.io/library/python:3.12.10-slim-bookworm@sha256:fd95fa221297a88e1cf49c55ec1828edd7c5a428187e67b5d1805692d11588db
      command: ["/bin/sh", "-ec"]
      args:
        - |
          printf 'nfs volume mounted\n' > /data/result.txt
          cat /data/result.txt
          rm -f /data/result.txt
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        readOnlyRootFilesystem: true
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: idleloom-nfs-smoke
